type checkOutTypes = {
  name: string;
  value: string;
  HandleChangeState: React.ChangeEventHandler<HTMLInputElement>;
  type: string;
  placeholder: string;
  language: string;
  region: string;
};

export type { checkOutTypes };
